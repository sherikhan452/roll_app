import 'package:flutter/material.dart';
import 'package:roll_app/gradient_color.dart';

main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        // backgroundColor: Colors.deepPurple,
        body: GradientColor(),
      ),
    ),
  );
}
